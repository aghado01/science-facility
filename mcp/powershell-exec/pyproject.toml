[project]
name = "powershell-exec-mcp"
version = "0.1.0"
description = "Client-neutral MCP server for executing PowerShell"
readme = "README.md"
requires-python = ">=3.10,<4"
dependencies = [
    "mcp<2",
]

[tool.uv]
link-mode = "copy"
package = false
