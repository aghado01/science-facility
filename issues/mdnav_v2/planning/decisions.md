# mdnav v2 — decisions register

One line each, ascending, with the reason and where it came from. This is
the audit trail for the design brief: if a later change contradicts a row
here, either the row is amended with a date or the change is wrong. Sources:
**G** = Gemini note 2026-08-16, **F** = fable-review 2026-07-29, **C** =
codex spec v0.1, **S** = Sol XOR note, **P** = para-agent
addressable-context-stream note, **T** = this thread 2026-08-17, **D** =
doccer README/source.

| # | decision | why | src |
|---|---|---|---|
| D1 | mdnav 2.0 generalizes the substrate; **nothing currently working is discarded** — every verb keeps CLI, output format, anchors, byte-fidelity covenant. | It is a foundation for the mdnav MCP, not a replacement product; goldens make this checkable. | T |
| D2 | A mask is an **index layer**, not a hiding mechanism: kinds are indexed independently so any reader can filter, isolate, count, compose. | Corrects rev-1 framing; enables profiles. | T |
| D3 | **Suppression is a query policy, never a claim property.** No `is_noise`/`is_mask` on claims; "noise vs prose" is a profile. | Doccer `Suppression.cs` verbatim; one fence suppresses headings under one query and is the target under the next. | D, T |
| D4 | The engine discovers **typed constructs as claims**; kind vocabulary is open; overlap and nesting preserved. | Documents contain typed objects; signal/noise is disposition. | T, D |
| D5 | Port from doccer: `SpanSet` (identity-forgetting algebra), claims/`SpanBatch` shape, `PatternRule` collectors with region-scoped execution, prefix parity, nearest-container, `RewritePlan`-shaped piece list. **Doccer is never a runtime dependency.** | Bazooka for a knife-fight; nothing calls .NET. | G, S, T |
| D6 | Not ported: Allen relations, `ClaimPairView`, path selection, facts/saturation, origins, vectors beyond parity, second segmentation policies. | Keep the knife a knife; separate contracts if ever needed. | T |
| D7 | Delimiter geometry is the unifying collector spec: **boundary / toggle / pair**; a spec is data `{pattern, geometry, scope, kind}`; built-ins are shipped specs; callers may pass one. Partitions are validated values. | Idiomatic Markdown constructs are recognizable by their delimiters; masking/isolating/segmenting are one operation with different arguments. | T |
| D8 | HTML blocks by CommonMark **block start/end conditions 1–7**, never open/close tag pairing; type 2 emitted as `html-comment`, not `html-block`. | Unclosed `<div>` in a transcript must not swallow the file; comment and block must not double-claim. | T (+ reviewer note) |
| D9 | `html-tag` (single tag, inner text kept) and `html-block` (whole block) are **two kinds**; "strip tags keep prose" (today's default) is a projection over the first. | Preserves README §Triage policy under the general model. | T |
| D10 | Footnotes (`[^l]`/`[^l]:`) and link reference definitions are **core kinds**, with keyed **relations** (`footnote`, `link-ref`, `anchor`, `contains`) returning pairs + residue. No `citation-ref` kind; no site-specific rule inventories for idiomatic Markdown. | Idiomatic Markdown, not a Perplexity disposition; the ref→def relation is what a preview renders as a click. | T |
| D11 | Nested headings: `# x` inside a region (html-block, blockquote) is a **heading claim with a container**; ids stay ordinal over all heading claims; activation = `level ≤ depth ∧ container chain transparent`; **`--enter <kinds>`** is the third knob (basis / depth / extent / *enter*); default `enter` empty; `outline` names what it does not descend into. | Nested, not flattened, not ignored; anchors do not move; nothing silently omitted. | T, S |
| D12 | Re-entry runs region-specific rules over the region window **in source coordinates**; no derived masters, no OffsetMap, no prefix stripping. | Doccer defers OffsetMap too; coordinates stay one space. | T, D |
| D13 | Generic basis `--by <kind | pattern:<re>>`; boundary bases address `Snnnn`, region bases `Rnnnn`; gaps between regions are units; `--within … --by` re-segments recursively; **first-viable-boundary is the only policy**. | The XOR walk in CLI form; tiling invariant per basis. | S, T |
| D14 | Claims engine is **in-memory first**; `MemoryStore` (server) and `SidecarStore` (one-shot CLI) share schema 3; views never persisted. | A server is a persistent process; sidecars are the CLI's cold cache. | T |
| D15 | **Persistence with rehydration, not accretion:** corpus-scoped `index/` (inventory + one `documents/Dnnn.json` per doc, replaced in place, ids stable across runs and restarts); run-scoped `<stamp>/reads.jsonl` + `run.json`; `runs prune --keep`. Work-dir resolution and the refusal guard unchanged. | N discoveries of one small doc must not leave N indexes; a run *is* provenance and stays stamped. | T |
| D16 | Front-end grammar mirrors `nu-skills`/`nu-modules`: list/read/search/inspect/status with **typed pure-content returns** (bytes, table, record); progressive disclosure; never prose. | Already running in `nushell-mcp`; mdnav maps verb-for-verb. | T |
| D17 | **REPL contract:** every query paged + counted (`{total, rows}`), memoized by `(digest, policy, args)`; `materialize` takes `maxBytes` and returns a **plan** (zero bytes) when over; bytes-or-table never both; previews capped at the engine; anchors are the agent's memory; session handles are plain data. | The caller's context is the scarce resource; `nu --mcp`'s `$history` is the model. | T |
| D18 | Stream framing: **front-declared, typed, addressed regions** — `¶ addr k/N kind basis ~size policy… span= len=`; header grammar is a **shared spec with para-agent** that mdnav conforms to; `--frame comment|pilcrow|none` as a projection over the piece list; `comment` = CLI default (byte-identical to today), `pilcrow` = MCP default; placeholders are addressable zero-`len` regions. | Documents and exchanges become one kind of object in the Primary's stream; declare-before-payload has a causal-attention rationale. | P, T |
| D19 | `len` (emitted UTF-8 bytes) and `span` (source geometry) are **always distinct fields**, both machine-facing; the model-facing magnitude is `k/N` + coarse size, plus — when a client token profile is loaded (D29) — a **measured token estimate** `~Nt` from composition × per-kind ratios; no profile, no token number. Byte `len` claims **no attention benefit**. | Byte-semantics trap; models do not count bytes; the boundary the model uses is the next sentinel. | T |
| D20 | The framing value proposition is **evaluated behaviorally** (framed vs unframed; address-recall, misattribution, tokens-to-answer) — a server-brief gate, not an assumption. | Attention is not observable from outside; carry the claim honestly. | T |
| D21 | mdnav 2.0 will be **vendored into para-agent** like `nushell-mcp`: engine single-file zero-dep; server embeddable `createMdnavTools({corpus, session, framing})` + thin stdio runner. | Para-agent supplies session/result store; addresses fit transcript rows. | T |
| D22 | **Rewrite the engine from the old file as figure model**: tests + goldens first, presentation/IO ported with intent, README as second figure model. v2 lives in `mcp/mdnav_v2/`; the legacy `skills/doc-dive/mdnav/` is the oracle and is **never edited**; repointing the doc-dive skill at v2 is a separate one-line decision at parity (2026-08-17: amended from "replace in one commit" after the move in `38710e0`). | The brief inverts control flow; retrofit would carry the old seams; a blank page would lose behaviors the brief does not restate. | T |
| D23 | **No parallel sub-agents.** Chip A (M0–M3, parity) → Chip B (M4–M5) → fresh-context review; sequential, worktree-isolated, reports appended to the brief. | One file, hard ordering, golden baseline must precede any change. | T |
| D24 | Fable-review F1–F4 and cosmetics are absorbed here (still open in code as of 2026-08-17), not filed again. | Same bugs, structural fix. | F, T |
| D25 | Cadence thresholds and `GapCadence` alignment are out of scope; report whether the claims refactor makes alignment free. | Not on the critical path. | T |
| D26 | Default MCP budget number is **not** chosen in the brief; the report proposes one from measured reads. | Tune against evidence. | T |
| D27 | Planning artifacts live in `issues/mdnav_v2/planning/` (this register, roadmap, figure-model survey); the brief remains design canon and is amended, not forked. | The brief had grown into canon + roadmap + method; split by role. | T |
| D28 | *(proposed — pending shared-spec freeze with para-agent)* Header glyph carries the **coarsest kind**: `§` document unit (section mark), `¶` exchange/turn or generic coding region (paragraph mark), at most one more (`†` foot-matter). Glyph set ≤ 3; `kind=` carries the fine kind; grammar otherwise unchanged. | Borrows an existing typographic prior instead of teaching a pattern in-context; both glyphs cheap and line-anchored; regularity preserved. | T |
| D29 | *(roadmap, after the server brief; shared with para-agent)* A **token-cost measurement battery** — model-agnostic, measured through the model (count endpoint or usage deltas), covering UTF-8 registers, the framing vocabulary *in situ*, and payload mixes; captured as client/model/date profiles under `profiles/tokens/`, refreshed when models change. In lieu of a standalone tokenizer. | Budgets, `~size`, and glyph choices should be honest in tokens; a tokenizer is not always available; BPE cost is contextual, so patterns are measured whole. | T |
| D30 | *(proposed — pending shared-spec freeze)* Framing is a **psr-style hybrid**: flat newline-delimited length-prefixed records (machine; `len` authoritative; elisions as sibling *pieces*, no nested arithmetic) + an optional model-facing **close line `§/ addr`** (re-mention, completeness, legible nesting; a checksum for the machine). Modes `comment | pilcrow | closed | none`; MCP default chosen by the D20 eval. Header field style (positional pipes vs `k=v`) decided at spec freeze after in-situ measurement (D29); lean: positional core, `k=v` for optional stamps. | Closure and length prefixing solve different problems for different readers; keeping them in separate lanes lets one grammar serve reposnapshot, para-agent and mdnav. | T |
| D31 | *(proposed — pending shared-spec freeze; supersedes the "frame mode" naming in D18/D28/D30)* The non-coding convention is a **sigil vocabulary + key**, per reposnapshot's method: length prefix is the framing authority, sigils are reader presentation; sigils **chosen by measurement** (line-start frequency, UTF-8 bytes, in-situ tokens, NFC/strip survival, visibility, semantic honesty); the sigil↔meaning **key declared once ahead of content** (adapter skill + first record of a session — a cipher key, not a decoder spec); candidates `§` unit · `¶` turn · `…` elided · `⁂` close (optional) · `†` foot-matter (if ever), set ≤ 5, fixed at freeze. Emission is profile data — `sigils: legacy-comment | typographic | none`, CLI `--sigils` (replaces `--frame`); close on/off by D20 eval; field style by D29. | Lightweight character convention in the coding/non-coding spirit rather than a heavy frame apparatus; the concept transfers from reposnapshot, the characters do not. | T, reposnapshot shard-format-notes §Sigil selection |
| D32 | *(amended by D34 — principles retained, verbatim adoption withdrawn)* **Row grammar borrows psr's principles** (`utils/reposnapshot/reposnapshot-v3/schema/psr.header.json`), for the payload format: LF record terminator, ` | ` field delimiter, `{…}` blocks one level deep, `<type>`/`<type:width>` in the key/header row, empty marker = nothing between delimiters, header row byte-identical per run; **the header row IS the grammar — rows are the header projected, no row schema** (anti-drift); `content_bytes` immediately precedes `content` (adjacency = seek contract) — mdnav's `len` takes that name; `span` stays mdnav's (one fact, one column); `content_meta {…}` is the open element model carrying mdnav composition/`~t`; **one physical line per row**, line breaks in content encoded by the **codec** (1:1 visible codepoint substitution) — never `
` escaping, which degrades legibility and grows token cost linearly with volume (the reason psr exists). Nothing is ever `k=v`. D29 measures row/codec cost; it does not choose the style. | psr's raison d'être is direct sequential consumption by a model, proven on real artifacts read into context; the LTS specimen read on 2026-08-17 (`…s024_hashish.txt`) was the escaped prototype, and a 2 KB sample is not evidence about volume — an earlier "comprehension intact" reading is withdrawn as evidence. | T, reposnapshot v3 |
| D33 | *(open — owned by the atomic payload format brief, see D34)* **Stream row terminator: `||`** (explicit row-close, ~1 token — a visible "row complete" and an unmistakable end to a text cell for an attention reader that cannot count bytes); psr *files* keep LF-only / no trailing delimiter (ledger #45); the two are reconciled deliberately at freeze. **`read` content cell: `content: codec | raw` is profile data** — `codec` (one physical line, psr-conformant, the candidate for the MCP default) vs `raw` (literal multi-line source of exactly `content_bytes`, required by the byte-fidelity covenant, gate 14); `legacy-comment` (CLI default) stays raw and byte-identical to today; the MCP default is decided by the D20 eval, not pre-decided. | Same piece list, two renderings; the seek reader and the attention reader differ on exactly these two points and nowhere else. | T |
| D34 | **Two things stay distinct: reposnapshot's artifact format (psr, a shard file consumed by seeking) and the in-context atomic payload format (a stream consumed by attention) — the second borrows the first's *principles*, never its transcription.** The payload format is its own brief (roadmap: "in-context atomic payload format"); rough shape `<sigil> | <source identity> | <semantic sub-address> | <token estimate> | <content> ||`; key-once vs self-identifying rows is open because the stream interleaves discontiguous chunks from different sources; `§`/`¶`/`…`/`⁂`, front-declared typed addressed regions, addressable elisions, and measured token estimates all survive; `||` vs LF and codec vs raw are decided there by D20/D29. MCP tool names are namespaced (`mdnav_*` or one namespaced tool) — no collisions with native harness tools. | This thread over-transcribed psr into the brief on 2026-08-17; the correction preserves the earlier design work instead of obliterating it. High-level concepts for roadmap and ideation, not spec. | T |
| D35 | **Payload framing P0 = per tool-call result.** The first prototype wraps every result the backend returns to the prompt (bytes or table) in one barebones structure — opening sigil, source identity (tool + document/corpus), semantic sub-address, magnitude (bytes; `~Nt` once profiled), content, close — so accumulated results from many tools and files are bounded, attributed, re-mentionable objects in the sequential stream. Cross-document/file interleaving rules, key-once vs self-identifying rows, codec vs raw, `||` vs LF, `⁂` are layered on P0 after use; the D20 eval runs against P0 first and P0 may be wrong. Key/legend delivered once via the adapter skill. | Scope discipline: solve the unit that does not depend on the general scheme; agents make many tool calls across many files and tools, and per-result framing is what gives self-attention reliable structure as those results accumulate. | T |
| D36 | **`briefs/span-layers-brief.md` (rev 3) is trimmed to canon** — doctrine, problem, five-primitive shape summary, non-goals, method, and the single master numbered exit-gate list (1–21b, phase briefs cite by number only, never restate gate text). Execution-ready spec moves to five sequential phase briefs, one per `roadmap.md` milestone group: `briefs/01-spanset-claims.md` (M1+M2), `02-collectors-parity.md` (M3, Chip A ends), `03-containment-queries.md` (M4, Chip B begins), `04-repl-contract.md` (M5a), `05-framing-p0.md` (M5b — see D37 for how its relation to para-agent is framed). Each phase brief gets its own Report section, appended by its own implementing agent; canon amends once all five close and M6 runs. **Amends D27**: "the brief remains design canon and is amended, not forked" meant no divergent copies, not no execution-scoped decomposition — the split follows gate/milestone boundaries `roadmap.md` already tracked, restates nothing. *(Superseded by D38 — the filename was not kept stable; the canon moved out of `briefs/` entirely.)* Review also found and fixed two drifts, corrected in `roadmap.md`: gate 12 (`coverage`/kept-citation-label fix) belongs to M4 per the brief's own Sequencing §step 6 and Chip-plan paragraph, not M3 as `roadmap.md`'s M3 gate list had had it; and the Chip-plan paragraph's "gates 17–21" prose had silently dropped 21b, which `roadmap.md` had correctly. M5 splits into M5a/M5b in `roadmap.md` to match. | rev 2 was one 886-line document mixing doctrine, per-milestone implementation trivia (win32 `isMain` guard, GFM slug dedupe, CommonMark HTML-block conditions), and ~200 lines of explicitly-flagged ideation (stream framing) at one altitude — unusable as a working spec for one chip without reading past the others; the project's own REPL-contract doctrine (D17, "a query never inlines more than the caller's budget; everything else is a handle") applies reflexively to its own design docs. `mcp/mdnav_v2/` is still empty at time of split — zero implementation switching cost. | T |
| D37 | **M5b (framing P0) is developed with para-agent's integration trajectory in mind, not gated on it.** Strikes the "blocked on the shared-spec freeze" / "external dependency" framing D36 gave M5b (in `roadmap.md` and `briefs/05-framing-p0.md`) — replaced with a plain trajectory note: build with para-agent's direction in mind, decide and ship the design as it stands here, reconcile later as its own roadmap item. D28/D30/D31 stay "proposed, pending freeze" as forward-looking design notes; that hedge is not escalated into a phase-blocking dependency. | mdnav_v2 and para-agent are both actively in development; a formal external dependency between two projects still in flux is a recipe for development frustration, not rigor — the awareness belongs in the design, not as a gate on this phase's exit. | T |
| D38 | **Canon relocated: `briefs/span-layers-brief.md` → `design/mdnav_v2_design-brief.md`.** It was never a proper brief to be executed — a design document that had accreted the material now split out into the five phase briefs (D36). `briefs/` now holds only execution-ready phase specs; `design/` holds canon (and future design-stage material that isn't yet an executable brief). All cross-references updated: canon's own links to the five phase briefs (`../briefs/0N-*.md`), each phase brief's pointer back to canon (`../design/mdnav_v2_design-brief.md`), and `roadmap.md`'s "Design canon" line. Historical discussion transcripts that mention the old path (`discussion/fable-mdnav_v2-design-session-*.md`, `discussion/gemini-value-proposition.md`) are left as-is — they are records of what was said at the time, not living pointers. | The user moved and renamed the file directly, correctly naming what D36 had already half-noticed: a design doc dressed up as a brief. | T |
| D39 | *(amends D15)* **Artifacts split by owner, no legacy mode, no global pointer.** Two roots: `$MDNAV_CACHE` (default `%LOCALAPPDATA%\mdnav\cache` / `~/.cache/mdnav`) holds the **IR** — the claims table + line stats + windows for a given byte content, **content-addressed** at `ir/v3/<sha[0:2]>/<sha256>.json`, engine-owned, disposable, GC'd by `cache prune`; `$MDNAV_WORK_DIR` (default `<dir of first target>/.doc-dive/`, only for calls that carry a path) holds the **investigation** — `inventory.json` (`Dnnn` ↔ path + sha/bytes/mtime as the fast-path invalidation row), `LATEST`, `runs/<stamp>/{run.json, reads.jsonl}`, owned by the session owner (a directory for the CLI, para-agent's store for the server). Resolution is **explicit or bust**: `--work-dir` > `$MDNAV_WORK_DIR` > path-anchored default; a `Dnnn` call with neither dies naming both. The legacy `$TMP/mdnav/LAST` pointer is dropped. Refusal guard applies to both roots. `LATEST` stays as the in-work-dir default for `--run`. | IR is a pure function of bytes, so content addressing removes invalidation logic entirely and lets CLI + server + overlapping corpora share one index; identity and provenance belong with the work, not the engine; "last thing used" state is discovery, not addressing — the user: explicit or bust. Nothing legacy did about layout is owed: if v2 does it, it is v2's behavior. Source: 2026-08-17 M0 capture discussion. | T, [M0 capture](../reports/m0-legacy-capture-20260817.md) |
| D40 | **`--json` on `discover`/`index` is the structural-facts seam.** Prints the per-document record (today's sidecar shape — `counts`, `noise`, `breaks`, `headings[]`, `windows[]`, `maxLine`, `newline`, `setextSuspects`, `frontmatter`, plus `id/path/sha256/bytes`) built from inventory row + IR. The legacy suite reads sidecar files directly in 17 places and inventory/ledger paths in ~15; at M0 those reads move to `--json` so the suite is black-box before any engine code exists, and M2 changes only the layout underneath. Same record is the typed return the MCP surface wants (D16); `legacyView(doc)` is the one function that produces it, and every ported formatter consumes it (M0 report §1). | Tests coupled to a file format are tests of the format; the CLI should state the facts it computes. | T, [M0 capture](../reports/m0-legacy-capture-20260817.md) |
| D41 | *(amends D11, D13; canon for the structural model is [design/mdnav_v2_structure-brief.md](../design/mdnav_v2_structure-brief.md))* **One recursive procedure discovers both hierarchies; the result is flat rows with structure in a path.** Per window: carve regions (opaque leaves + containers, state-driven; parity for residue only) → mask → partition the mask by headings → recurse down the spine (level+1) and into enterable containers (switched context, source coordinates) → collect inline leaves over the mask. A second, inside-out construction (parent = smallest strictly-containing span; sections by stack walk per window; per-node tiling) must agree; disagreement is residue (`crossing`, `escape`, `unclosed`+`alternative`, `setext-suspect`, `unaligned`, `dangling`/`unused`), reported with the alternative reading, never repaired. Storage is one flat table per document; rows overlap by design; three identities per row — `ord` (reading order, this build), `path` (structural address; root spine `Hnnnn` flat, nested `…/q1/H1`, projections `…/S3`/`…/W2`, placeholders `…/elidedN`), `digest` (title content). `resolve()` takes a full path or a unique suffix, so legacy `Dnnn:Hnnnn@dig` anchors still resolve. Coverage stays byte-based. The reader's surface is a telescope — `tree`/`read`/`find`/`status`, each `tree` row carrying a census of what is under it — never chunks; framing wraps results. Non-goal sharpened: no *inline* parser; block structure by shape and block conditions is the model. Gates 8/10b reworded, 8b (agreement) and 8c (selection identity) added. | The nested-heading numbering argument was a symptom of a flat address grammar bolted onto a tree; naming nodes by path under their parent makes the spine stable under engine evolution and lens choice, and makes "give me section 1" a selection whose two forms (path prefix, span containment) checking equal is the correctness gate. Reposnapshot's shard captures nesting in a path field the same way. | T (2026-08-17 M0 capture discussion), S (XOR walk), reposnapshot psr |
| D42 | **Fence is a `pair`, never a `toggle`, and `<!--`…`-->` is a `pair`.** A `pair` collector's state machine *proposes* a delimiter only when its match condition holds — for fence, same closer char, length ≥ opener's, empty info string — so a `~~~` line inside an open ```` ``` ```` fence is content, never a delimiter candidate; prefix parity over the machine's own **emitted** claims is used only afterward, to report an odd carry-out as residue, never to decide which lines are delimiters. `toggle` is reserved for genuinely symmetric single-token pairs with no stateful match condition (`$$`…`$$`). *Rider, mechanical, no new gate:* the master exit-gate list (canon, `mdnav_v2_design-brief.md` §Exit gate) is confirmed the sole holder of gate text — the structure brief (D41) states only which gates it rewords/adds and points there, matching the D36 discipline; and gate 16's struck "3.5 MB real corpus" clause (agreed 2026-08-17, recorded in the M0 report) is applied to the canon text itself, naming `issues/mdnav_v2/discussion/` as the real-document set. | Brief 02 as filed described fence as prefix-parity over every fence-looking line, which XOR-folds a `~~~` inside an open backtick fence into a false toggle and cannot pass gate 7 on a mixed-fence document — caught reviewing the phase-brief edits against D41's construction (§2 "carve"), which requires the *carve* step to decide membership before any parity math runs. | T ([plan review](../reports/fable-planning-briefs-review-20260817.md) §3.2) |
| D43 | *(amends D41's projection-code summary; supersedes D13's `Snnnn`/`Rnnnn` global addressing, already node-scoped by D41, with the specific S/R/W split below)* **A row-field query language replaces per-verb ad-hoc flags** (`--only`, `--for`, `--in`/`--not-in`, "strip a comma-list of kinds"). Every claim row is queried by combining predicates over its independent fields: `path` (`=` / `under` prefix / `glob`), `kind` (`in`), `level`, `span` (`contains`/`within`), `ord` (range), `title` (`~`), `digest` (`=`), derived tags (`has dangling`, …), `census` (`has <kind>`), and a relation join `via <relation> <node>` (rows on the far side of a named relation from a node's rows — the general form of the retired `--for`). **Scope** (`--within`, `path under`), **selection** (the predicates), and **disposition** (`select` vs `ignore`, optionally from a lens) are three separable concerns the CLI's one-flag-per-verb design had conflated. `select`/`ignore` take an **ordered predicate list**, last-match-wins, `!` reversing the immediately preceding match (rescue under `ignore`, un-keep under `select`) — ported from reposnapshot's `rs.core.membrane.psm1` `GlobCompiler`: one compiled list, one dual-truth-table interpretation at the call site, never two code paths; fail-fast on an empty or self-annihilated `select` list. **`path glob`** ports that module's `TranslateGlob`/`CompileGlobs` char-by-char translator unchanged — mdnav paths are `/`-separated and kind-coded, the same shape applies (`**/q*` = every blockquote, `H0007/**` = everything under section 7) — with one **named departure**: membrane prunes an excluded directory so nothing under it is ever re-included; mdnav cannot, because a match implies its subtree **by span**, not by directory pruning, so every node is tested independently and a `!` on a descendant carves its span back out geometrically (a citation island inside an ignored `<div>` stays rescuable). **`read` accepts multiple node/predicate arguments in one call and returns one packet:** the union of their spans (`SpanSet`, so overlapping requests never double-emit), assembled in **document reading order** (`ord`) regardless of argument order, each contiguous piece carrying the address(es) that produced it — the general form of today's `--headings a,b,c` decoration. `--strip`/`--only`/`--keep` retire as primary spellings; `--strip all` (bare, or `--strip=all`) is kept as a **golden-only alias** for `--ignore @default` and is the only value `--strip` accepts going forward — which also retires F4's remaining parse-time-vocabulary problem (2026-08-17 review §3.7) as moot, since `--strip` no longer takes a kind list. **Correction to D41's projection-code summary:** three codes, not two — `S` (boundary/pattern bases, e.g. `--by breaks`), `W` (windows), and `R` for a **region-kind basis's gap pieces only** (`--by fence`; the region pieces themselves keep their own claim path, `/H0007/f1`, not a synthetic number) — matching D13's original S/R distinction, node-scoped. | Rows carry multiple independent fields that should combine freely rather than fuse into one path mechanism; scoping, filtering and provenance are different questions; reposnapshot had already solved "one compiled predicate list, two dual interpretations" for this exact shape of problem, and region-kind bases genuinely differ from boundary/window bases (some pieces are existing claims, not synthetic), which the two-code summary had lost. | T (2026-08-18 discussion), reposnapshot `rs.core.membrane.psm1` (GlobCompiler ported for `path glob` and the select/ignore dual semantics; island-rescue departure named) |
| D44 | **The disposition concept — strip/keep/enter/triage as named, loadable, composable data — is renamed `lens`, going forward.** `--profile` / `$MDNAV_PROFILE` / `mcp/mdnav_v2/profiles/*.json` become `--lens` / `$MDNAV_LENS` / `mcp/mdnav_v2/lenses/*.json`; the `lenses` verb replaces a would-be `profiles` verb. **Unaffected, both keep "profile":** the census verb `profile` (composition/cadence of a document — unrelated meaning), and D29's client token-cost **profiles** (`profiles/tokens/<client>-<model>-<date>.json` — a third, also-unrelated meaning). Historical rows D1–D38 that used "profile" for the disposition concept are **not** rewritten — a dated record of the term in use at the time; `lens` is used from D39 onward and in the active briefs/canon. | Three unrelated things were sharing one word — a reader's noise/prose disposition, a census verb, a client's measured token cost — confusing in conversation and in eventual tool names; free to fix while `mcp/mdnav_v2/` is still empty. | T (2026-08-18 discussion) |
| D45 | *(corrects gates 21/21b as written in the design brief and `briefs/05-framing-p0.md`)* **Framer arithmetic is per-glyph and per-row, and the P0 gate only asks for what P0 decides.** (a) The "header byte length == char length + 1 for every sigil, incl. the 3-byte `…`/`⁂`" clause was self-refuting: 2-byte `§`/`¶` give +1, 3-byte `…`/`⁂`/`†` give +2. The assertion becomes `Buffer.byteLength(prefix) - prefix.length` == Σ (UTF-8 length − 1) over the prefix's non-ASCII codepoints, asserted as a per-glyph fixture table rather than one constant, and scoped to the **row prefix** — the content cell is arbitrary UTF-8 (3-byte Control Pictures under `codec`), so no constant delta exists for a whole row. (b) The byte-total identity counts field delimiters **and the row terminator** (` ||` + LF in the stream, LF alone in files) inside the non-content term; brief 05's paraphrase "Σ header bytes" had dropped them, which is exactly the leak that makes a plan's `framed` total disagree with what a read writes. (c) Gate 21's `--sigils none` byte-for-byte equality is qualified to `--content raw`; under `codec` the equality is on the **decoded** cells. (d) **Gate scope split:** 21 keeps what P0 ships (every result framed incl. single-anchor, one row per contiguous piece ordered by `ord` with addresses that resolve back, `content_bytes` exactness under `typographic`+`raw`, `legacy-comment` golden byte-identity); new **21c** carries codec round-trip, elision rows, the `⁂` close, `||` vs LF, key-once, and cross-source interleaving to the atomic payload format brief, settled with the D20 eval. | A gate that demands certification of what the phase's own D35 scope section defers is not checkable, and an arithmetic gate that states the wrong arithmetic would ratify the bug it exists to catch. | T (2026-08-18 discussion) |
