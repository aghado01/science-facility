---
format: Structural
exported_at: 2026-08-20T01:23:38.5558475Z
exchanges: 5
user_label: Aipithicus
sessions:
  - 6f13b040
models:
  - claude-opus-5
exclude:
  - thinking
  - synthetic
  - timestamps
  - session-markers
  - exchange-markers
  - tool-calls
  - tool-results
  - subagents
---

# ive lost track of my progress on the procurement module and associated MCP development. i was working on it intensely and stepped back for a few days and im trying to look with fresh eyes

Here's where the procurement lane actually stands. Short version: **you left it at a clean, closed checkpoint** — nothing is half-finished, and the MCP is live.

## Where you stopped

Last procurement commits were **Aug 15** (`b0cfd97` uv update, `b433882` README follow-up). Working tree is clean on procurement — the only uncommitted work in the repo is doccer/pdf-prose.

That last piece of work was the **uv bootstrap + MCP registration** chain: `brewery/uv/` (pin.json, uv.lock, `restore-uv.ps1`, `write-mcp-registration.ps1`, `verify-install.py`), witnessed by `tests/infrastructure/uv-bootstrap.Tests.ps1` and `tests/mcp-servers/procurement_mcp/test_subprocess.py`. `scientiae-procurement` is now in [.mcp.json](.mcp.json) running through pinned offline uv. I called it live this session — it answers.

## Verified state

- **Suite green**: procurement + procurement_mcp = **199 passed / 1 skipped / 98 subtests**, 12.8s.
- **Catalog**: 13 source-ready articles with `article.json` sentinels; `inventory.jsonl` in sync.
- **Providers**: openalex, semanticscholar, arxiv, zenodo live; scihub registered with **zero capabilities** (declaration only).

## The Aug-12 review is fully remediated

Every item on the addendum's 7-step action order landed across `7ea1626 → 032dfd0 → 8aa259a → 573e050`:

| Item | State |
|---|---|
| 3 silently-inert Windows tests + junction reparse witnesses | converted |
| `DepositSlug` contract honesty | closed via `BeforeValidator(validate_deposit_slug)` (not the pattern tightening the review suggested — the guard now runs at the protocol layer instead) |
| `HttpClient.get` redirect confinement, artifact `content-encoding` refusal | landed, +202 lines of `test_http.py` |
| Five-shield / currency / safety consolidation | `runtime/concurrency.py`, `storage/safety.py` |
| Protocol-path tests for acquisition tools | **all 16 MCP tools** now exercised through the protocol |
| Sci-Hub + Crossref deferral briefs, `html` README sentence | minted |
| `.mcp.json` registration | done |
| `CatalogSettings.name` slug validation asymmetry | fixed |

## What's actually still open

**Deliberate deferrals** (stated in the READMEs, tracked in briefs):
1. **PDF-only article profile** — the biggest capability gap. A lone PDF can be receipted but cannot mint `article.json`; needs its own versioned source-form profile so it doesn't weaken the LaTeX contract by exception.
2. **Sci-Hub fetch** and **Crossref resolver** — briefs exist, no adapters.
3. **Async fetch jobs** — deferred pending a lifecycle contract. Note the two `arxiv-async` briefs still describe the retired PowerShell MCP, not the Python lane.
4. **`html` form** — acquirable and receipted, no consumer.
5. **Q13 is still your decision**: batch ingestion of never-receipted tarballs lives only in `src/logistics/latex-source-batch.ps1`. A Python "receipt an already-present tarball" path would subsume it.

**Concrete evidence of #1 and #5 sitting in your inventory right now** — 6 of 19 directories have no sentinel:
- PDF-only, blocked on the PDF profile: `2410.02707v4`, `2607.09648v1`, `2607.11883v1`
- **PDF + tar.gz present but never receipted**, blocked on Q13: `2608.06370v1`, `2608.09867v1`, `2608.11095v1`

Those three tarball dirs are the Q13 decision made physical.

**Process gap**: `issues/procurement/` has **no planning canon** — no `planning/{decisions,roadmap,ledger}.md` triple like doccer and TeXdig have. That's precisely why re-orienting cost you a dig through two READMEs and an 833-line review doc.

## Two environment notes

The pytest temp poisoning is still real, plus a wrinkle I hit: conftest now requires `--basetemp` under `artifacts/`, explicit `TEMP`/`TMP`/`TMPDIR`, and `PYTHONDONTWRITEBYTECODE=1` — and the temp dir must **not** live inside the basetemp tree (pytest wipes it mid-run; that produced 31 phantom failures on my first attempt). Working invocation:

```bash
TEMP=D:/aghado01/codex-scientiae/artifacts/procurement-status/env TMP=$TEMP TMPDIR=$TEMP PYTHONDONTWRITEBYTECODE=1 .venv/Scripts/python.exe -m pytest tests/procurement tests/mcp-servers/procurement_mcp --basetemp=artifacts/procurement-status/pytest-tmp -q
```

I've updated the procurement memory to current truth (it still said registration was pending).

**Suggested next move**: mint `issues/procurement/planning/{decisions,roadmap,ledger}.md` capturing the above, then take the PDF profile — it's the item with the most downstream pull, and it connects to the pdf-prose work sitting untracked in your tree. Want me to draft the planning canon?

---

# what about the agent onboarding extraction and grok integration

That's a different repo — both live in **science-facility / para-agent**, not codex-scientiae. Here's the state.

## Grok: three tracks, all still open, and they're gated

The planning canon at `issues/para-agent/planning/` (minted `d111056`, Aug 15) reframed Grok — decision **P11**: *Grok is the extensibility acceptance test, not a delivery wave*. Named tracks replaced the colliding Wave 0–4 numbering (**P15**).

**`grok-isolation`** — independent, no code dependency, and the only thing standing between the substrate and Grok. Ruled **no-go** on Aug 15 (**P10**): exact unsandboxed inspection found four inherited stdio MCP definitions while `mcp list --json` returned empty — so the empty listing proved nothing. Open question is whether a clean-config invocation, explicit empty allowlist, or documented no-start flag actually prevents *load and start*. Evidence: `issues/para-agent/briefs/grok-1.0.4-wave0-evidence.md`. **No authenticated turn, model call, or session artifact until this closes.**

**`substrate-migration`** — independent of isolation, six steps, everything lands atomically. Step 6 (golden plans) is **blocked on P18** — readiness-version normalization must be decided first or the goldens are machine-dependent from birth.

**`grok-onboarding`** — requires *both* of the above. Not started: **zero occurrences of "grok" anywhere in `mcp/para-agent/src`, `config`, or `mcp/tests`**. No integration profile, host binding, session profile, or adapter fixture exists.

## What *is* built

The client-integration substrate landed (`447d03c` + `cbce45a`, 6,408 insertions): 12 modules under `mcp/para-agent/src/client-integration/` (registry, environment, policy, invocation, readiness, workspace, carrier, scavenger, semantic-json…) and 4 JSON schemas — adapter, host-binding, integration-profile, session-profile. Bounded gate at `8fe3643`: **21 suites / 218 discovered / 218 passed**. Production mediation is still on the *old* path; the cutover is the migration track.

## On "onboarding extraction"

I read that as the substrate-migration work — pulling client-specific onboarding out of generic services so a client is added by **profiles and codecs only**. That's exactly P11's success criterion: *"Grok was added through profiles and codecs only, with zero changes to generic services"* — and the roadmap states that any generic-service change forced during onboarding is a **substrate defect**, and that finding is worth more than the client. The extraction step that hasn't happened is migration step 1: **remove command authority from adapters** (adapters keep encoding, parsing, correlation, native projection only).

If you meant something narrower by "extraction," say the word and I'll go straight at it.

## One thing to fix cheaply

The planning canon's links are stale. `decisions.md` and `ledger.md` point at `../reports/` for three files that actually live in `../briefs/`:

- `grok-1.0.4-wave0-evidence.md` (cited twice)
- `sol-remediation-swarm-plan.md`
- `fable-sol-remediation-plan-review.md`

The ledger even asserts "Both historical plans moved to `../reports/` with superseded headers" — they're in `briefs/`. Three link rewrites, or move the files; your call which direction is canon.

## Where the work actually stopped

Para-agent commits ended Aug 15–16 (canon mint, module bucketing, nushell pulled into its own MCP). Since then your commits moved to **mdnav_v2 briefs** (Aug 17, `02b76e1`) and reposnapshot v3 — which has two uncommitted modified files right now. So both procurement and para-agent are parked at clean checkpoints; the live edge is reposnapshot/mdnav.

The natural next move on this track is **grok-isolation**, since it needs no code and unblocks the acceptance test. Want me to work that probe, or fix the canon links first?

---

# i've moved the the read to science-facility. lets do the canon stale links and then maybe do some documentation house cleaning. im overdue and the struggle is real with the clutter right now

The canon isn't the thing that's wrong — **P15 is a ruling** (`reports/` holds point-in-time reports and superseded plans; `briefs/` holds runstamped per-chip guidance), and the ledger records the move as done. The files just never moved. Confirming evidence: `sol-remediation-swarm-plan.md` already sibling-links `mediated-transcript-and-nu-engine-architecture.md`, which lives in `reports/` — that link is broken from `briefs/` and correct from `reports/`.

So the fix is to move the files, not rewrite the links.
All para-agent planning/reports/briefs links now resolve. Committing that before the wider sweep:
Every broken target resolves to a real file — they're all depth relics from when these docs lived one level up at `issues/para-agent/`. Repairing them:
`sed -i` silently converted CRLF→LF on all 11 files — that's an unintended byte change beyond the link fixes. Reverting and redoing with a CR-preserving method:
Two more relic classes surfaced (non-`contract/` mcp paths, and repo-root-relative links). Every corrected target resolves, so extending the table and re-running from the original blobs:
Both link commits are in. Now sizing the wider clutter — surveying the other topics before proposing:
Both link commits landed. Here's the wider clutter picture, measured rather than eyeballed.

## What I found across `issues/`

| Topic | files | lines | planning canon | broken links (live tiers) | broken (chat logs) |
|---|---|---|---|---|---|
| para-agent | 51 | 31,879 | ✅ decisions/roadmap/ledger | **0** (just fixed) | 80 |
| reposnapshot | 50 | 11,770 | ⚠️ non-standard¹ | **85** | 12 |
| mdnav_v2 | 21 | 7,307 | ⚠️ no ledger | **56** | 29 |
| mcp | 11 | 6,769 | ❌ none | **38** | 11 |
| nushell | 5 | 266 | ❌ none | 4 | 0 |
| pwsh-exec | 2 | 116 | ❌ none | 0 | 1 |
| doc-dive | **0** | 0 | — | — | — |

¹ `decisions-ledger.md` (merged), `payload-manifest-ledger.md`, `v3-consolidation-plan.md` — doesn't match the decisions/roadmap/ledger triple P15 rules for.

**183 broken links in live tiers.** Same relic class I just fixed in para-agent — docs authored at a flatter layout, then filed into subdirs. Mechanically fixable with the same verify-every-target-first method.

## Structural inconsistencies

- **Tier name split**: para-agent uses `discussions/` (what P15 names); mcp, mdnav_v2, reposnapshot, pwsh-exec, doc-dive use `discussion/` singular.
- **`archaelogy/`** — misspelled in both mdnav_v2 and reposnapshot (should be `archaeology/`).
- **`issues/doc-dive/`** is empty — one empty `discussion/` dir, zero files.
- **`issues/antigravity-ignorefile.md`** sits loose at the root, no topic.
- **`para-agent/.archive/`** holds a chat log with 2 broken links — hidden tier, no other topic has one.

My read on the chat logs: they're verbatim evidence records, and rewriting links inside them edits the record. I'd leave all ~130 of their broken links alone. But that's your call.
All three taken. Doing renames first so the link repair sees the final layout.
The dry-run's "DEAD" bucket is mostly my regex catching non-links (`file:///` URLs, stray `](text)`). Tightening the matcher:
All 42 verified-correct, no ambiguous cases. Applying:
Targets are fixed; three link *labels* still print the misspelling. Correcting those (label should match the path it points at), leaving the misspelling where it's narrative inside review reports:
The rename broke two links I'd fixed earlier in para-agent. Patching those:
Done. Three commits, all verified.

## What landed

**[b33785d](.) — the relocation the ledger already claimed**
P15 rules that `reports/` holds point-in-time reports and superseded plans, and the ledger's canon-mint entry records both historical plans as moved there. They never left `briefs/`. Moved the four files the canon cites; that alone fixed the 4 broken planning-canon links with no edit to the canon itself.

**[8c5b512](.) — reports/ link repair + CRLF restore**
Every report under `para-agent/reports/` was authored one level up at `issues/para-agent/`, so its relative links were off by a segment. ~90 links repaired across five relic classes (cross-repo depth, `../../mcp/`, `../../utils/`, repo-root-relative, and sibling files that had moved into `reports/`).

**[13e63ad](.) — repo-wide normalization**
`discussion/ → discussions/` in four topics, `archaelogy/ → archaeology/` in two, plus 42 more link repairs in mcp, mdnav_v2, reposnapshot.

Every relative link in every live tier now resolves. Chat-log tiers untouched, as you chose.

## One thing I broke and fixed

My first pass used `sed -i`, which **silently converted CRLF→LF** on all 11 files it touched — a 14-link fix became a 286-line diff, and b33785d committed a line-ending change on two files. `core.autocrlf=false` and no `.gitattributes` here, so those were real byte changes, not a checkout artifact.

Redone byte-wise from the original git blobs with a per-file CRLF/LF assertion, and the earlier damage is reverted in 8c5b512. Final check: **zero line-ending drift** across every edited file. I've added this to the bulk-edit memory alongside the PowerShell array-flattening lesson — same failure class, different tool.

## Seven dangling references — reported, not guessed

These point at things that genuinely don't exist, so no rewrite was safe:

- `issues/mcp/reposnapshot-57cf060b….md` → `rs.core.sharding.psm1` (×3) and `rs.core.lsh.psm1`. v3 now has `rs.core.shards.psm1`, and `rs.lts.sharding.psm1` survives in the LTS lane — plausible successors, but that's a claim about what the doc meant, so it's yours to make. Separately, that file is a UUID-named session export sitting loose in `issues/mcp/` rather than in a `discussions/` tier.
- `mdnav_v2/reports/m0-legacy-capture-20260817.md` → `./fig.png`, never committed.
- Two machine-local absolute paths (one malformed: `C:\Users\azrie.claude\…`, missing a separator).

## Structural gaps still open

Untouched — these need authoring decisions, not mechanical fixes:

| Topic | Gap |
|---|---|
| `issues/mcp` | 11 files, 6,769 lines, **no planning canon at all** |
| `issues/nushell`, `issues/pwsh-exec` | no planning canon |
| `mdnav_v2` | has decisions + roadmap, **no ledger** |
| `reposnapshot` | merged `decisions-ledger.md` + two strays instead of the P15 triple |
| `issues/` root | `antigravity-ignorefile.md` still loose, no topic |
| `issues/doc-dive/` | still empty (untracked, so invisible to git) |

`reposnapshot` is where I'd start — 50 files and 11,770 lines is the largest body of work in the repo without a proper roadmap, and you have uncommitted changes in `rs.core.membrane.psm1` and `shards.contract.json` right now, so it's the live edge. Want me to mint its canon from the existing `decisions-ledger.md` and `v3-consolidation-plan.md`?

---

# i've committed the reposnapshot work, lets do some information gathering and minting then housecleaning to archives. its alot to comb through

Rich ledger — decisions #1–48 are all there. What's missing is the roadmap and the completed-work ledger. Reading the consolidation plan and the work arc:
The battery is **not green** — `container.tests.ps1` fails at load (0 passed, 1 failed). Getting the cause:
Everything I need. Minting the canon now — starting with the roadmap:
The write landed but my line-ending assertion was wrong-headed (I added lines, so counts legitimately change). Checking whether the file is CRLF and I've mixed terminators:
Canon is minted and links are clean. Now the archives pass — inspecting the ambiguous small docs:
Clear classification. Checking the archive convention and inbound links before moving:
Committed clean. Recording the canon locations in memory:
Committed as [0fda669](.). Working tree clean.

## What I found combing through

**The decisions ledger was already the good part** — #1–48, with reasons and reversal costs, status-tracked. It didn't need rewriting, just renaming to `decisions.md` and pointing at its new siblings. What was missing was everything *forward* and everything *behind*: the consolidation plan was carrying a status registry, a work log, and a forward plan in one 615-line document, which is exactly the stratigraphy failure P15 was written against.

**Where v3 actually is:** green through **assemble** (golden-validated against a live LTS monolith, 2026-07-29). Export is the frontier — container landed 2026-08-17, and `rs.core.shards.psm1` and `rs.core.serialize.psm1` are **0-byte placeholder files**. Shards has a complete brief and settled packing decisions; serialize has neither.

## One thing you'll want to know

**The battery is not green.** `tests/run-all.ps1` reports **16 suites · 937 passed · 1 failed** — `container.tests.ps1` aborts at its first case.

Cause: your 08-18 spec restructure renamed `schema/psr.header.json` → `contracts/container.spec.jsonc` and moved the column register under `shard_container_schema`. `Resolve-Layout` still reads top-level `$decl.columns`, gets null, and indexes into it. Mid-migration, not a defect — the spec change simply hasn't reached the module.

The arithmetic confirms nothing else regressed: 937 + container's 70 asserts = the 1007 recorded at the container landing. That's the roadmap's first item.

## Minted

`planning/` is now the standard triple plus the payload registry:

- **`decisions.md`** — renamed, intro rewired
- **`roadmap.md`** — ahead only, named tracks: container-spec-realignment (blocking) · export-e2e · encoding-lookback · open calls owed · side quests · deferred
- **`ledger.md`** — completed, newest first, seeded from the work log, changelog, and code arc, with counts tied to the commits they were observed at
- **`payload-manifest-ledger.md`** — left alone; it tracks what the payload owes a *reader*, which is a different axis from completed work
- `v3-consolidation-plan.md` → `reports/` with a superseded header

## Archived

- `gemini-shard-recon.md` + `fable-shards-pre-brief.md` → `archaeology/`. The live shards brief already cited the recon *as* archaeology, and the pre-brief self-labels "record, not live guidance." They're one episode with sibling cross-links, so they only stay valid moving together.
- Your `issues/mcp/reposnapshot-57cf060b-*.md` → `.archive/` move is now staged and committed.
- Added `.archive/README.md` matching the `briefs/.archive/` convention — what each retired log is, and why it's there rather than in `discussions/`.
- `AGENTS.md` doc-tier section rewritten for the new canon, the `schema/` → `contracts/` move, and `container.spec.jsonc`.

## Left deliberately

- **`memories/project_reposnapshot_contract.md`** — a non-standard tier holding one exported Claude memory. Content is still true (never-fail ingest, handlers are transformers not parsers). Its one link is malformed *and* points at the **ThermoMapper** memory dir rather than science-facility. Editing an exported memory copy seemed low-value; say the word if you want it folded into `design/` or repointed.
- `design/numerics-roadmap.md` and `class-implementations.md` stay in `design/` — both are live guidance for the export track. I pulled numerics' conclusions into the roadmap's Deferred section with a pointer back.

Next natural move is the container realignment — it's small, it unblocks the battery, and a red gate makes every subsequent claim about shards harder to trust. Want me to take it?

---

# <command-message>chat-export</command-message>
<command-name>/chat-export</command-name>
<command-args>to `D:\aghado01\science-facility\issues`, i muxed up this thread across topics</command-args>
