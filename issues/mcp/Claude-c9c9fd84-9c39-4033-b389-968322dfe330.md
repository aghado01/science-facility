---
format: Structural
exported_at: 2026-08-19T19:59:31.5305407Z
exchanges: 10
user_label: Aipithicus
sessions:
  - c9c9fd84
models:
  - claude-opus-5
  - claude-sonnet-5
exclude:
  - thinking
  - synthetic
  - timestamps
  - session-markers
  - exchange-markers
  - tool-calls
  - tool-results
  - subagents
---

# ok i need a clean read on this `D:\aghado01\science-facility\issues\para-agent\briefs\cursor-remediation-read-ahead.md`it's based on `D:\aghado01\science-facility\issues\para-agent\briefs\sol-client-integration-updates-20260414.md`, its feeling a bit confused

---

# <command-name>/model</command-name>
            <command-message>model</command-message>
            <command-args>claude-opus-5</command-args>

---

# <local-command-stdout>Set model to claude-opus-5</local-command-stdout>

`cursor-remediation-read-ahead.md` is **0 bytes** — empty file, untracked, created 02:19 today. There's nothing in it to read. No para-agent session is running that might be mid-write (`list` returns zero panes), and no copy of that filename exists anywhere under `D:\aghado01`.

So whatever you're reacting to as "confused" is the source brief, not the read-ahead. Here's the clean read on **[sol-client-integration-updates-20260414.md](../../science-facility/issues/para-agent/briefs/sol-client-integration-updates-20260414.md)**, which I did read in full.

## What's actually incoherent in it

**1. The ledger says nothing has landed. Git says both waves landed.**
W0 and W1 both sit at `State: ready-to-commit`, and the file's own rule is "The containing commit anchors the entry." But:
- W0's specified commit `para-agent: freeze client integration contract` exists as `d601288` — landed.
- W1's substrate almost certainly landed as `cbce45a para-agent client integration` (message doesn't match the brief's specified `para-agent: add client integration substrate`).

Both ledger entries are stale-by-one. That alone makes the doc read as "we're stuck before W0."

**2. The Reviewed baseline block was never refreshed, contradicting its own rule.**
It says "Review HEAD: `d0b7984` on `main`, eight commits ahead of `origin/main`." Actual HEAD is `8fe3643`, **16 ahead**. `d0b7984` is now ~20 commits back. The rule two lines below — "Root refreshes HEAD, branch divergence, versions, and worktree state at every wave start" — has not been honored.

**3. The W2 exit gate cites a superseded test count.**
W2-E says "original **165** plus all new manifest tests pass." But the header (line 14) and the W1 ledger both establish **21 suites / 216**. 165 was the W0-era number. A W2 worker reading W2-E literally will gate on the wrong baseline. This is the single most likely source of "confused."

**4. The version-authority section was just rewritten and left a dangling seam.**
Your uncommitted diff flipped Claude from *"needs fresh 2.1.232 evidence or a genuinely pinned 2.1.226 executable before W2 can claim live compatibility"* to *"live tests use the current executable; no pin required."* But downstream:
- W2-C still asserts "Claude/Codex **fixtures** and golden plans remain exact" — fixtures are 2.1.226, live is 2.1.232, and the doc no longer says what happens when a golden plan diverges under 2.1.232.
- W0's Unverified list still carries "Claude 2.1.232 protocol compatibility" as an open item, which the new policy neither closes nor re-scopes.

The policy changed; the gates that depended on the old policy didn't.

**5. Checkbox discipline is inconsistent.**
W0-A…D carry `[x]`. W1-A/B/C carry no checkbox at all — only `[x] Append W1 status before commit` is marked, yet top-level W1 is `[x]`. W1's exit is asserted in prose only.

**6. Filename date doesn't parse.**
`...-20260414` reads as 2026-04-14 under house runstamp canon, but every ledger entry is 2026-08-15. Either a transposition or a stamp from a different document's lineage.

## What I need from you

The empty file is the actual blocker. Which is it:

- **Something failed** — a Cursor agent was supposed to write that read-ahead and produced nothing, and you want me to find out what happened; or
- **It's a placeholder** — you touched the path and want me to write the read-ahead: a short, self-contained brief that hands Cursor the W2 remediation with the six items above already resolved (corrected baseline, 216 gate, closed version-authority seam), so it doesn't have to reconcile a 371-line brief against a drifted ledger.

If it's the second, tell me whether the read-ahead should *carry* the corrections or *instruct Cursor to fix the source brief first* — those produce very different documents.

---

# fixed. read read-ahead now

Ultracode is on, but your CLAUDE.md resolves that conflict explicitly — when UltraCode is toggled you want the primary agent's reasoning, not subagent dispatch. So I'm doing this solo and deep. The read-ahead makes five code-level claims (`supported_versions`, `expected_version`, `verified_versions`, `assertApplicationVersion()`, `NATIVE_APPLICATION_VERSION_MISMATCH`) — a read-ahead that names identifiers is only useful if they're real. Verifying all of them.
Identifiers all check out. Now the two load-bearing behaviors — what readiness does with an *empty* allowed-version list, and where `NATIVE_APPLICATION_VERSION_MISMATCH` is actually raised.
One more — whether the safe invocation descriptor carries the readiness `version` fact. If it does, the no-pin default collides with W2-A's exact-descriptor equality and the golden plans.
Read it. It's **substantively correct on its core claims** — I verified every one against the code — but it has one false statement, one misdescribed-and-unimplemented error code, and one unaddressed collision that will bite the W2 worker.

## Verified correct

| Claim | Evidence |
|---|---|
| `supported_versions` omittable | [client-integration-profile.schema.json:35](../science-facility/mcp/para-agent/src/schemas/client-integration-profile.schema.json:35) — `executable.required` is `["id"]` |
| `expected_version` omittable | [client-host-binding.schema.json:27](../science-facility/mcp/para-agent/src/schemas/client-host-binding.schema.json:27) — `required: ["path"]` |
| Omit both → "a 2.1.232 box just runs" | [readiness.js:389](../science-facility/mcp/para-agent/src/client-integration/readiness.js:389) — the drift check is guarded by `effectiveAllowedVersions.size > 0`. Empty set ⇒ dimension passes and records observed. [client-invocation.test.js:177](../science-facility/mcp/tests/para-agent/client-invocation.test.js:177) already exercises this with `2.1.232` |
| Pin set → still fails closed | Same guard, plus [invocation.js:200](../science-facility/mcp/para-agent/src/client-integration/invocation.js:200) rejects an incoherent pin with `INVOCATION_PLAN_INVALID` |
| `verified_versions` doesn't gate projection | [adapter-conformance.test.js:219-228](../science-facility/mcp/tests/para-agent/adapter-conformance.test.js:219) already proves projection reports observed `2.1.232` against the `2.1.226` fixture |
| Live projection never calls `assertApplicationVersion()` | Only caller anywhere is [adapter-conformance.test.js:44](../science-facility/mcp/tests/para-agent/adapter-conformance.test.js:44). Zero src callers |

## Three problems

**1. Line 7 is false as written.** "It no longer blocks registry load" — `verified_versions` **presence** is still a hard gate. [client-adapter.schema.json:38-42](../science-facility/mcp/para-agent/src/schemas/client-adapter.schema.json:38) requires it with `minItems: 1`, and [registry.js:76](../science-facility/mcp/para-agent/src/client-integration/registry.js:76) throws `adapter_profile_invalid` if it isn't an array. What stopped gating is its *contents* — it no longer participates in the version intersection. A Grok or AGY profile author who reads line 7 literally and omits the field will fail registry load.

**2. `NATIVE_APPLICATION_VERSION_MISMATCH` is misdescribed, and it doesn't exist yet.**

Line 9 says "if one turn sees two different versions." That's not what it means. [errors.js:50](../science-facility/mcp/para-agent/src/client-integration/errors.js:50) and [CLIENT-INTEGRATION-CONTRACT.md:56-58](../science-facility/mcp/para-agent/contract/CLIENT-INTEGRATION-CONTRACT.md:56) both define it as **receiver-native version vs launch readiness** — cross-layer, not two native observations.

More importantly: it has **zero throw sites and zero tests**. It exists only as a definition and as contract prose. "Unchanged" is true in the sense that it's unchanged at *unimplemented* — but a W2 worker reading "that is still a real fault" will assume enforcement is there.

And this is the part worth stating explicitly: with pins off, this is now the **only** remaining version gate on the live path, and per the contract it's grouped under *Accepted execution* with `retryable: false`. So the no-pin default silently moves version disagreement from a **pre-acceptance preflight failure** to a **post-acceptance turn kill**. That's a real risk transfer, and it's invisible in the current text.

**3. The golden-plan collision is unaddressed — this is the one that will actually break W2.**

[finalizeInvocationDescriptor](../science-facility/mcp/para-agent/src/client-integration/invocation.js:292) embeds `readiness: clone(checkedReadiness)`, and [assertReadinessDescriptor](../science-facility/mcp/para-agent/src/client-integration/readiness.js:660) copies every dimension's facts — including `version: { version: <token> }`. **The durable safe descriptor carries the observed version string.**

With no pin, that field is machine-dependent. W2-C demands "Claude/Codex fixtures and golden plans remain exact"; W2-A demands descriptors "match exactly." Intra-turn acceptance-vs-terminal is fine (same snapshot). But any golden capturing a full descriptor now differs between a 2.1.226 box and a 2.1.232 box. "Bounded fixtures can keep their labelled versions" doesn't resolve it — the goldens need an explicit rule: inject synthetic readiness, or normalize `readiness.version.version` before comparison.

**Also silent on:** [registry.js:235](../science-facility/mcp/para-agent/src/client-integration/registry.js:235) throws `adapter_unverified` unless `verification.status === "verified"`. AGY is "intentionally unverified" and has its own W2 worker. The doc covers version gates but not the gate that will actually block that lane.

## Document-level

- **No title, date, or scope line.** It opens mid-thought at "Launch now follows the installed executable."
- **"now" and "no longer" have no referent.** A fresh Cursor agent never saw the superseded rule, so these are diffs against an invisible baseline. State them as present-tense fact, and name the retired rule once if the delta matters.
- **"Your configs will not use it"** — second person, no antecedent, and redundant with line 11, which says the same thing imperatively and better.
- **Filename over-promises.** `cursor-remediation-read-ahead` implies W2 remediation broadly; content is version authority only. Either retitle to `version-authority-read-ahead.md` or absorb the rest of W2.
- **No source link, no actionable checklist, no exit gate.** Only line 11 tells the reader to do anything.

Want me to rewrite it with the three corrections folded in and the golden-plan rule made explicit? I'd keep it roughly this length — the concision is right, it's the precision that's off.

---

# yea there's so much confusion across these briefs, i kind of want to read them for design intent against the code and rewrite a new sequence with fresh eyes. this impetus for all of this was me wanting to introduce grok and realizing that it was time to reel some infrastructure and abstraction for handling clients and onboarding them. which lead to a redesign of things in discussed in this brief. there was also an earlier fable review `D:\aghado01\science-facility\issues\para-agent\briefs\fable-sol-remediation-plan-review.md` . so there's a mess and i want to clean it up

This is the right instinct — the briefs have drifted into contradiction and patching them individually will keep producing seams like the three I just found. Let me ground in the full corpus before proposing anything.

Ultracode is on, but your CLAUDE.md resolves that in favor of primary-agent reasoning over subagent dispatch, so I'm reading this myself rather than fanning out.
`planning/` is **empty**. That's the root cause, and it reframes everything.

Your own README says planning is "canon for planned roadmap and sequencing, design decisions ledger, implementation log, codified architecture and design specs." Nothing is there. So `sol-client-integration-updates-20260414.md` has been forced to be a design spec, a wave plan, *and* a status ledger simultaneously — which is exactly why it drifts against itself and against the code.

Reading the origin intent and the current frozen contract now.
The contract is the key discovery — and it changes the diagnosis.

[CLIENT-INTEGRATION-CONTRACT.md](../science-facility/mcp/para-agent/contract/CLIENT-INTEGRATION-CONTRACT.md) is **good**. 388 lines, internally coherent, and lines 46–60 *already* state the version-authority rules correctly and more precisely than the read-ahead does. Which means the read-ahead is a patch on the drift of a lossy paraphrase (the brief's "Frozen design" section) of a document that was already right.

Now reading the earlier remediation plan — I suspect a numbering collision.
Now I have the whole picture, and the two plans collide in a way that explains everything. Let me get one piece of ground truth before I propose — whether the claimed test baseline is real at HEAD.
Bounded gate is green — and it just proved the point.

```
PARA_MANIFEST_SUMMARY {"suites":21,"discovered":218,"completed":218,"passed":218,"failed":0,"skipped":0,"cancelled":0}
```

**218, not the 216 the W1 ledger claims.** Two tests appeared after someone hand-copied the number into prose. That's the disease in miniature.

## What's actually true right now

- Bounded suite: 21 suites / 218 passing, zero failures, at HEAD `8fe3643` with the version-authority edit uncommitted.
- The contract trio in [mcp/para-agent/contract/](../science-facility/mcp/para-agent/contract/) — mediated-exchange, console, client-integration — is **canon, and it's good**. Coherent, precise, honest about what's unproven.
- `issues/para-agent/planning/` is **empty**, though your README designates it as canon for roadmap, decisions ledger, and implementation log.

## Why it feels confused — five mechanisms, all structural

**1. Two wave sequences, both numbered 0–4.** `sol-remediation-swarm-plan.md` runs W0 contract-freeze → W4 release. `sol-client-integration-updates-20260414.md` runs W0 contract-freeze → W4 release. Different work, identical labels. "Wave 2" means *vertical slice* in one and *atomic client migration* in the other. Both have a `verification`/`release-audit` worker in W4. Nothing disambiguates them but the filename.

**2. The briefs restate the contract in lossy paraphrase.** The brief's "Frozen design" section is a compressed retelling of CLIENT-INTEGRATION-CONTRACT.md. The moment the contract moves, the paraphrase is wrong — and the fix goes into the *paraphrase*, not the contract. That's precisely what produced the read-ahead: a patch on the drift of a restatement of a document that was already correct. Contract lines 46–60 state version authority better than the read-ahead does.

**3. The briefs carry mutable status ledgers.** A document can't be *frozen design* and *live status* at once. That's why W0 and W1 both still read `State: ready-to-commit` when both landed (`d601288`, `cbce45a`).

**4. The briefs carry their own historical audits and appended reviews.** The remediation plan opens with a historical-plan notice, then the original audit, then the Fable review, then a reconciliation section that says the first three are no longer true. Four strata in one file, and you have to read to line 156 to reach current state.

**5. Hand-copied counts drift.** 115 → 165 → 216 → actually 218. Four numbers, three of them stale, all embedded in prose that no runner validates.

## The good news: the design intent is intact

I traced it back to your own note in [sol-client-loading.md:10-16](../science-facility/issues/para-agent/notes/sol-client-loading.md:10), which predates both plans:

> "The abstractions should remain composable rather than becoming one profile per client/shell/application combination" — surface policy, shell profile, application adapter, host environment binding, invocation receipt.

Those five axes map **one-to-one** onto the contract's authority table: `SessionProfile`/surface, shell-only-for-panes, `ApplicationAdapter`, `HostBinding`, and the safe invocation descriptor. Your governing rule — "invocation mode is explicit and owned by para-agent, never inferred from ambient shell behavior or TTY detection" — is what the whole compiler/plan split enforces. Even your three loose user notes landed: child-process inheritance, PATH/env overrides (with the `Path`/`PATH` collision rule), and "not necessarily json" (contract: "JSON Schema/JSON is the first provider, not a permanent domain dependency").

**Nothing is confused about the design. Only the paper.** That's a much smaller problem than it looks like.

## The reframe I'd propose for the sequence

Grok is currently Wave 3 of five — a payload delivered *after* the substrate. I think that's backwards, and it's why the sequence feels like it's dragging you away from what you actually wanted.

**Grok isn't a wave. Grok is the acceptance test for the substrate.** W4-C already says the extensibility gate is "add a fake fifth client through profiles/codecs only." Grok *is* that client. Collapsing them makes the sequence honest about why the substrate exists, and puts your actual goal at the end as the proof rather than as one more deferred item.

And critically — **the Grok blocker has no code dependency.** W0-B went no-go because unsandboxed inspection found four inherited stdio MCP definitions while `mcp list --json` returned empty, with no proven no-load control. That's a config/isolation question about the grok CLI. It doesn't need the substrate, doesn't need W2, and can be settled independently, right now, in parallel with everything else. It's currently buried as a prose condition inside a wave that can't start for two more waves.

Deduplicating both plans plus the gaps I found in the code, the real remaining work is four tracks, not ten waves:

| Track | Content | Depends on |
|---|---|---|
| **Grok isolation probe** | Settle the no-load/no-start question, or decide Grok never gets managed mediation | nothing |
| **Substrate migration** | Adapter command-authority removal, compiler injection, strict `spawn` union, v1/v2 persistence, three profiles atomically | nothing |
| **Honesty gaps** | `NATIVE_APPLICATION_VERSION_MISMATCH` implemented-or-demoted; golden-plan determinism vs machine-dependent `readiness.version`; AGY fresh stream capture; live Windows recert; the egress post-commit question | migration |
| **Grok onboarding** | Profiles/codecs only, zero generic-service changes — which *is* the extensibility proof | probe + migration |

Three of those four items in "honesty gaps" are already admitted as open in the remediation plan's release gates and have been carried unresolved across both documents.

Before I write canon, three forks are genuinely yours:
All three recommendations taken. Let me match your existing house format for planning canon rather than inventing one.
House format captured. Reading the ledger shape, then I'll write the canon.
Now precise. `447d03c` — labelled "wave 0" — is actually the W1 substrate (6,408 insertions). `cbce45a` is the version-authority rework the read-ahead documents, and it landed *after* the W1 ledger entry was written, which is exactly the +2 that makes 216 into 218.

Writing the canon.
Now the moves. Note that `fable-sol-remediation-plan-review.md` is also a report — and its content is duplicated verbatim inside the swarm plan's `# Fable Review` section, so it goes too.
Now the superseded headers so neither can be mistaken for current.
That swept the read-ahead in — exactly the accidental staging the remediation plan warned about. Unstaging it, since its disposition is still your call.
Commit message picked up a stray `@` — I used PowerShell here-string syntax in the Bash tool. Checking and fixing.
Committed as `d111056`. Recording the canon location for future sessions.
Done and committed as `d111056`.

## What's now in place

**[issues/para-agent/planning/](../science-facility/issues/para-agent/planning/)** — canon, in your TeXdig house format:

- **[decisions.md](../science-facility/issues/para-agent/planning/decisions.md)** — 8 standing doctrine points, P1–P20, plus a Superseded section. Opens with an authority rule: the contracts are design canon, this file records *rulings and supersessions and never restates a contract rule*. That's the fix for the drift mechanism, stated as policy so it doesn't recur.
- **[roadmap.md](../science-facility/issues/para-agent/planning/roadmap.md)** — ahead-only, four named tracks, with the Deferred list carried forward from both old plans so it can't fall out.
- **[ledger.md](../science-facility/issues/para-agent/planning/ledger.md)** — newest-first, back to the 2026-08-13 ontology draft. Every count carries the commit it was observed at.

The three historical documents moved to `reports/` with superseded headers that name their specific stale claims, so anyone who lands on one knows immediately what not to trust.

## The thing that changed while I was reading

Two of the open items I found in the code aren't cleanup — they're **blockers on the migration track**, and they weren't visible as such in either old plan:

- **P17** — `NATIVE_APPLICATION_VERSION_MISMATCH` needs deciding *before* migration, not after. Making pins optional (P6) turned it into the only remaining live version check, and it's specified as post-acceptance. So a version disagreement now kills an accepted turn instead of failing preflight — and it has zero throw sites and zero tests, so today it does nothing at all.
- **P18** — the safe descriptor embeds `readiness.version`, which optional pins just made machine-dependent, while the contract still demands exact descriptor comparison and deterministic plan fixtures. Golden plans written before this is resolved will be environment-dependent from birth.

Both are in the roadmap with that ordering made explicit.

## One decision left with you

`briefs/cursor-remediation-read-ahead.md` is still there, untracked — I deliberately kept it out of the commit (it got swept in by a directory-wide `git add` and I unstaged it).

Its content is now fully absorbed: the accurate parts are P6 plus the `cbce45a` ledger entry, and the three problems I found in it are gone rather than propagated. Contract lines 46–60 already say it better. My recommendation is to **delete it** — it's a paraphrase of a paraphrase, which is the exact pattern P15 exists to prevent. But it's yours and I won't remove it without your word.

Bounded gate re-verified green at 21 suites / 218 before the commit. Nothing I touched is code.

---

# i think grok should be startable now

Time has passed and the memory picked up a repo-wide tier reorganization. Let me re-ground on what the prior probe actually established before re-running anything.
Grok is unchanged at `1.0.4 (d846eb93d9)` — same build as the no-go evidence. So if something changed, it's environmental. Let me find out what, starting with where those four inherited server definitions actually come from.
Config lives at `~/.grok/config.toml`. Let me look at the actual config state — my previous command had a syntax error.
`~/.grok/config.toml` has **no MCP section at all** — so those four definitions are inherited from compatibility locations. Let me find the actual source, reading only server names (these files carry credentials).
Node needs Windows paths in Git Bash. Codex config already shows three of the four (`filesystem`, `fetch`, `git`).
Found the source. `~/.cursor/mcp.json` contains exactly `filesystem`, `fetch`, `git`, `pwsh_exec` — the precise four the no-go probe found. Grok reads the Cursor compatibility location. Now: is there a documented control that stops it?
`grok mcp disable` exists, and there's a Claude Code Compatibility section. Let me read what grok actually discovers and whether config home can be redirected.
