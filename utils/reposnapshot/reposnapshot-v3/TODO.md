want to build ergonomic utilities for LM tool users

- eventual MCP layer
  - reposnapshot as an agentic tool that can be deployed to specifications by an agent on-the-fly
  - mcp introduces fluency, affordances and some guidance on how to effectively utilize reposnapshot for repo-level analysis or multiple file code analysis
  - specialized perceptual layer for efficient and detailed reasoning over large corpora
  - should be a relatively simple set of tools
  - make querying and fetching with byte offsets easy with mcp tools
  - fetch a subset of the data based on a list of byte spans e.g. across files first and later within files after subaddressing and pagination come into play
  - enable light semantic search on the virtual database via hashing methods
